const popupData = {
  ssbmobile: {
    kicker: "Stealth Security Architecture",
    title: "Stealth Safety Bank Mobile System",
    subtitle: "ระบบปกป้องเงินและข้อมูลระดับสูง ที่ออกแบบมาเพื่อตัดความเสี่ยงตั้งแต่ต้นทาง และแยกอุปกรณ์ธุรกรรมออกจากการใช้งานทั่วไปอย่างชัดเจน",
    lead: "Stealth Safety Bank Mobile System ไม่ใช่เพียงอุปกรณ์ แต่คือแนวคิดใหม่ของความปลอดภัยทางการเงินเชิงโครงสร้าง โดยเน้นการลดพื้นผิวการโจมตี แยกเครื่องสำหรับธุรกรรมโดยเฉพาะ และควบคุมการใช้งานให้อยู่ในขอบเขตที่จำเป็นจริง เพื่อทำให้ความเสี่ยงจาก phishing, malware, remote access และพฤติกรรมใช้งานปะปนลดลงอย่างมีนัยสำคัญ",
    badges: [
      "แยกเครื่องธุรกรรมโดยเฉพาะ",
      "ไม่มี browser / social / app store",
      "ลด Attack Surface",
      "ใช้งานเฉพาะเมื่อจำเป็น"
    ],
    features: [
      "ตัดช่องทางที่ไม่จำเป็นต่อธุรกรรมออกตั้งแต่ต้น ทำให้เครื่องไม่ถูกใช้ปะปนกับการท่องเว็บ โซเชียล หรือกิจกรรมที่เพิ่มความเสี่ยง",
      "ยกระดับความปลอดภัยด้วยการใช้งานแบบมีวินัย เปิดเฉพาะตอนทำธุรกรรม และปิดเมื่อไม่ใช้งาน เพื่อลดโอกาสการเข้าถึงโดยไม่ได้รับอนุญาต",
      "เปลี่ยนความปลอดภัยจากการพึ่งพาแอปเพียงอย่างเดียว ไปสู่การออกแบบโครงสร้างการใช้งานที่ลดความเสี่ยงทั้งระบบ"
    ],
    impacts: [
      { value: "ลดเสี่ยง", label: "จาก phishing, malware และการเข้าถึงระยะไกลที่ไม่เกี่ยวข้องกับธุรกรรม" },
      { value: "ชัดเจน", label: "แยกมือถือธุรกรรมออกจากมือถือใช้งานทั่วไปอย่างเป็นระบบ" },
      { value: "มั่นใจ", label: "ทำธุรกรรมได้ภายใต้กรอบการใช้งานที่ควบคุมง่ายและตรวจสอบได้" }
    ],
    useCases: [
      "ผู้ที่ต้องการความปลอดภัยสูงในการถือครองเงินและทำ Mobile Banking",
      "นักลงทุน เจ้าของธุรกิจ และผู้ที่ต้องดูแลหลายบัญชีหรือยอดเงินจำนวนมาก",
      "ผู้ที่เคยมีประสบการณ์หรือมีความกังวลเรื่องคอลเซ็นเตอร์ ฟิชชิง หรือการถูกหลอกให้ติดตั้งแอป"
    ],
    video: "https://www.youtube-nocookie.com/embed/mD9i39genW8",
    videoNote: "วิดีโอตัวอย่างจากช่อง @siamtham แสดงแนวคิดระบบในรูปแบบที่น่าเชื่อถือและไม่เปิดเสียงรบกวนอัตโนมัติ"
  },

  stealthbank: {
    kicker: "Integrated Protection Concept",
    title: "Stealth Safety Bank",
    subtitle: "แนวคิดการป้องกันทางการเงินที่เน้นความลับ การอำพราง การลดการเปิดเผยข้อมูล และการออกแบบวิธีใช้งานให้ปลอดภัยกว่าการพึ่งพาเครื่องมือเพียงชิ้นเดียว",
    lead: "Stealth Safety Bank คือกรอบแนวคิดที่รวมการป้องกันเชิงพฤติกรรม เชิงกายภาพ และเชิงเทคโนโลยีเข้าไว้ด้วยกัน เพื่อสร้างระบบความปลอดภัยที่รอบด้านมากกว่าแนวทางทั่วไป โดยมุ่งลดโอกาสการโจมตีตั้งแต่ต้นทาง ไม่ใช่รอป้องกันเมื่อปัญหาเกิดขึ้นแล้ว",
    badges: [
      "ลดการเปิดเผยข้อมูล",
      "อำพรางการจัดเก็บ",
      "ป้องกันหลายชั้น",
      "ยึดความรอบคอบเป็นหลัก"
    ],
    features: [
      "ไม่มองความปลอดภัยเป็นแค่เรื่องซอฟต์แวร์ แต่เชื่อมโยงวิธีคิด วิธีเก็บ และวิธีใช้งานเข้าด้วยกัน",
      "ลดความโดดเด่นของทรัพย์สินหรืออุปกรณ์สำคัญ เพื่อทำให้เป้าหมายถูกสังเกตเห็นยากขึ้น",
      "เน้นการสร้างความปลอดภัยเชิงโครงสร้างที่ต่อเนื่องและนำไปใช้ได้จริงในชีวิตประจำวัน"
    ],
    impacts: [
      { value: "รอบด้าน", label: "ครอบคลุมข้อมูล อุปกรณ์ และพฤติกรรมการใช้งานภายใต้ระบบเดียวกัน" },
      { value: "อำพราง", label: "ลดการสังเกต ลดการดึงดูดความสนใจ และลดโอกาสการเข้าถึง" },
      { value: "เป็นระบบ", label: "ยกระดับจากการป้องกันเฉพาะจุด ไปสู่แนวทางที่เชื่อมโยงทั้งกระบวนการ" }
    ],
    useCases: [
      "บุคคลหรือครอบครัวที่ต้องการวางระบบป้องกันเงินและข้อมูลอย่างรอบคอบ",
      "ผู้ที่ต้องการแนวคิดความปลอดภัยที่มากกว่าการติดตั้งแอปหรือซื้ออุปกรณ์ทั่วไป",
      "องค์กรที่ต้องการแนวทางการใช้งานที่สร้างวินัยและลดความผิดพลาดจากคน"
    ],
    video: "https://www.youtube-nocookie.com/embed/mD9i39genW8",
    videoNote: "วิดีโออ้างอิงจากช่อง @siamtham เพื่อให้เห็นภาพแนวคิด Stealth ในบริบทการใช้งานจริง"
  },

  safetybook: {
    kicker: "Physical Stealth Storage",
    title: "Stealth Safety Book",
    subtitle: "อุปกรณ์จัดเก็บเชิงอำพรางที่ออกแบบให้ดูกลมกลืนเหมือนหนังสือทั่วไป เพื่อลดการสังเกตและเสริมความปลอดภัยทางกายภาพให้กับอุปกรณ์หรือของสำคัญ",
    lead: "Stealth Safety Book มีบทบาทสำคัญในการปกป้องอุปกรณ์และสิ่งของสำคัญผ่านแนวคิดการอำพรางภายนอกให้ดูธรรมดา ลดโอกาสการถูกสังเกตหรือระบุว่าเป็นจุดเก็บทรัพย์สิน โดยทำงานเสริมกับระบบ Stealth Safety Bank เพื่อสร้างความปลอดภัยทั้งเชิงกายภาพและเชิงพฤติกรรม",
    badges: [
      "อำพรางเหมือนหนังสือ",
      "ลดการถูกสังเกต",
      "เก็บอุปกรณ์สำคัญได้เป็นสัดส่วน",
      "เสริมระบบ Stealth"
    ],
    features: [
      "ช่วยทำให้จุดจัดเก็บไม่โดดเด่นและไม่สะดุดสายตาเหมือนกล่องเซฟทั่วไป",
      "เสริมความปลอดภัยทางกายภาพให้กับอุปกรณ์ธุรกรรม เอกสาร หรือสิ่งของที่ต้องการความเป็นส่วนตัว",
      "เหมาะกับผู้ที่ต้องการเก็บของสำคัญอย่างเรียบร้อย แนบเนียน และใช้งานคู่กับระบบหลัก"
    ],
    impacts: [
      { value: "แนบเนียน", label: "ภายนอกดูเป็นของใช้ทั่วไป ลดการตั้งข้อสังเกตจากบุคคลรอบข้าง" },
      { value: "เป็นระเบียบ", label: "ช่วยจัดเก็บอุปกรณ์และของสำคัญให้พร้อมใช้งานอย่างมีวินัย" },
      { value: "เสริมชั้นป้องกัน", label: "ทำงานร่วมกับระบบความปลอดภัยหลักเพื่อเพิ่มความครบถ้วน" }
    ],
    useCases: [
      "ใช้เก็บอุปกรณ์สำหรับธุรกรรมที่ต้องการความเป็นส่วนตัว",
      "ใช้เก็บเอกสารหรือของมีค่าที่ไม่ต้องการให้ถูกพบง่าย",
      "ใช้ในบ้านหรือสำนักงานที่ต้องการความปลอดภัยแบบไม่ดึงดูดความสนใจ"
    ],
    video: "https://www.youtube-nocookie.com/embed/mD9i39genW8",
    videoNote: "วิดีโอตัวอย่างจากช่อง @siamtham ใช้เป็น reference สำหรับรูปแบบการนำเสนอและพฤติกรรมการเล่นวิดีโอ"
  },

  technology: {
    kicker: "Technology Overview",
    title: "Technology Overview",
    subtitle: "ภาพรวมของเทคโนโลยีและแนวคิดที่ทำงานร่วมกันเพื่อยกระดับการปกป้องเงิน ข้อมูล และการใช้งานในชีวิตจริงให้ปลอดภัยขึ้นอย่างเป็นระบบ",
    lead: "Technology Overview คือภาพรวมขององค์ประกอบทั้งหมดในระบบ ไม่ว่าจะเป็นการแยกอุปกรณ์ การลดช่องทางเสี่ยง การอำพรางการจัดเก็บ การเตือนสติ และการออกแบบพฤติกรรมการใช้งานให้รอบคอบขึ้น เพื่อให้แต่ละส่วนสนับสนุนกันและสร้างความปลอดภัยที่ต่อเนื่อง ไม่ใช่แยกกันทำงานแบบกระจัดกระจาย",
    badges: [
      "Multiple Layer Protection",
      "Device Separation",
      "Behavior Design",
      "Stealth + Discipline"
    ],
    features: [
      "เชื่อมการป้องกันหลายชั้นเข้าด้วยกัน ทั้งเชิงเทคโนโลยี เชิงกายภาพ และเชิงพฤติกรรม",
      "ทำให้ผู้ใช้มองเห็นภาพรวมว่าทำไมการลดแอป การแยกเครื่อง และการอำพรางจึงช่วยลดความเสี่ยงได้จริง",
      "ออกแบบให้เข้าใจง่ายขึ้นสำหรับการอธิบายต่อยอดไปยังครอบครัว ทีมงาน หรือองค์กร"
    ],
    impacts: [
      { value: "เชื่อมโยง", label: "องค์ประกอบแต่ละส่วนทำงานหนุนกัน ไม่ใช่แยกเป็นชิ้น ๆ" },
      { value: "รอบคอบ", label: "ช่วยให้ตัดสินใจใช้งานภายใต้หลักการที่ชัดเจนและตรวจสอบได้" },
      { value: "เข้าใจง่าย", label: "อธิบายระบบที่ซับซ้อนให้อยู่ในรูปแบบที่ผู้ใช้จับต้องได้" }
    ],
    useCases: [
      "ผู้ที่ต้องการศึกษาว่าระบบป้องกันประกอบด้วยอะไรบ้างและทำงานอย่างไร",
      "ผู้ที่ต้องการอธิบายระบบให้ทีมขาย ทีมงาน หรือผู้เกี่ยวข้องเข้าใจง่ายขึ้น",
      "ผู้ที่ต้องการประเมินแนวทางก่อนตัดสินใจใช้งานจริง"
    ],
    video: "https://www.youtube-nocookie.com/embed/mD9i39genW8",
    videoNote: "วิดีโอตัวอย่างจากช่อง @siamtham ใช้เพื่อแสดงภาพรวมเทคโนโลยีและแนวทางการสื่อสาร"
  },

  aboutcompany: {
    kicker: "Company Overview",
    title: "About Us",
    subtitle: "บริษัทผู้พัฒนาแนวคิดและระบบ Stealth Safety Bank Mobile เพื่อยกระดับความปลอดภัยทางการเงินจากประสบการณ์ปัญหาจริง และต่อยอดเป็นโซลูชันที่ใช้งานได้จริง",
    lead: "บริษัท สยามทำ จำกัด มุ่งเน้นการพัฒนาแนวคิด องค์ความรู้ และรูปแบบการใช้งานที่ช่วยลดความเสี่ยงด้านการเงินและข้อมูลส่วนบุคคล โดยผสานมุมมองด้านเทคโนโลยี ความรอบคอบในการใช้งาน และการจัดเก็บเชิงอำพรางเข้าด้วยกัน เพื่อทำให้ความปลอดภัยไม่ใช่แค่คำโฆษณา แต่เป็นระบบที่นำไปใช้ได้จริง",
    badges: [
      "บริษัทไทย",
      "พัฒนาแนวคิดจากปัญหาจริง",
      "เน้นใช้งานได้จริง",
      "มุ่งสู่โซลูชันระดับมืออาชีพ"
    ],
    features: [
      "พัฒนาองค์ความรู้ด้านการป้องกันความเสี่ยงทางการเงินด้วยแนวคิดที่ชัดเจนและต่อยอดได้",
      "ให้ความสำคัญกับทั้งความเข้าใจง่าย ความรอบคอบ และความน่าเชื่อถือของระบบ",
      "มุ่งพัฒนาระบบให้เป็นทางเลือกสำหรับผู้ใช้ที่ต้องการความปลอดภัยมากกว่ามาตรฐานทั่วไป"
    ],
    impacts: [
      { value: "พัฒนา", label: "นำประสบการณ์และปัญหาจริงมาสร้างเป็นระบบที่มีทิศทางชัดเจน" },
      { value: "ต่อยอด", label: "ต่อยอดแนวคิดเป็นองค์ความรู้ อุปกรณ์ และรูปแบบการใช้งานที่จับต้องได้" },
      { value: "น่าเชื่อถือ", label: "สร้างภาพลักษณ์มืออาชีพและชัดเจนสำหรับการสื่อสารกับลูกค้าและพาร์ตเนอร์" }
    ],
    useCases: [
      "ผู้ที่ต้องการรู้จักบริษัทก่อนตัดสินใจใช้งานหรือร่วมงาน",
      "ผู้ที่ต้องการเข้าใจทิศทาง แนวคิด และที่มาของระบบ Stealth Safety Bank",
      "ผู้ที่ต้องการข้อมูลเบื้องต้นเกี่ยวกับบริษัท สยามทำ จำกัด"
    ],
    video: "https://www.youtube-nocookie.com/embed/mD9i39genW8",
    videoNote: "วิดีโอตัวอย่างจากช่อง @siamtham ใช้เป็นภาพสะท้อนแนวทางการนำเสนอแบรนด์และระบบ"
  }
};

const agentMockData = [
  { name: "Agent Premium Bangkok", area: "Bangkok", code: "AG-BKK-001" },
  { name: "Somchai SSB", area: "Nonthaburi", code: "AG-NB-014" },
  { name: "Siam Safety Agent", area: "Bangkok", code: "AG-BKK-022" },
  { name: "Agent Taling Chan", area: "Bangkok", code: "AG-BKK-031" },
  { name: "SSB North Team", area: "Chiang Mai", code: "AG-CNX-004" },
  { name: "Agent Eastern Secure", area: "Chonburi", code: "AG-CBI-002" }
];

const $ = (selector, root = document) => root.querySelector(selector);
const $$ = (selector, root = document) => Array.from(root.querySelectorAll(selector));

const body = document.body;
const techHero = $("#techHero");
const logoMenuButton = $("#logoMenuButton");
const centerPulse = $("#centerPulse");
const nodeNote = $("#nodeNote");
const themeToggle = $("#themeToggle");

const infoPopup = $("#infoPopup");
const infoTitle = $("#infoTitle");
const infoVideo = $("#infoVideo");
const infoSubtitle = $("#infoSubtitle");
const infoLead = $("#infoLead");
const infoBadges = $("#infoBadges");
const infoFeatures = $("#infoFeatures");
const infoImpacts = $("#infoImpacts");
const infoUseCases = $("#infoUseCases");
const popupKicker = $("#popupKicker");
const videoNote = $("#videoNote");

const companyPopup = $("#companyPopup");
const contactButton = $("#contactButton");

const agentSearchForm = $("#agentSearchForm");
const agentSearchInput = $("#agentSearchInput");
const agentDropdown = $("#agentDropdown");
const agentResults = $("#agentResults");

const chatWidget = $("#chatWidget");
const chatToggle = $("#chatToggle");

let lastFocusedElement = null;
let audioCtx = null;
let resizeTimer = null;

function isMobileView() {
  return window.innerWidth <= 760;
}

function pulseCenter() {
  if (!centerPulse || isMobileView()) return;
  centerPulse.classList.remove("active");
  void centerPulse.offsetWidth;
  centerPulse.classList.add("active");
}

function syncHeroLabels() {
  const isOpen = techHero?.classList.contains("open");
  nodeNote?.classList.toggle("hidden", !!isOpen);
}

function lockBodyScroll(lock) {
  body.style.overflow = lock ? "hidden" : "";
}

function renderBadges(items = []) {
  if (!infoBadges) return;
  infoBadges.innerHTML = items.map(item => `<span class="popup-badge th-font">${item}</span>`).join("");
}

function renderFeatures(items = [], target) {
  if (!target) return;
  target.innerHTML = items.map(item => `<li class="th-font">${item}</li>`).join("");
}

function renderImpacts(items = []) {
  if (!infoImpacts) return;
  infoImpacts.innerHTML = items.map(item => `
    <div class="impact-card">
      <div class="impact-value th-font">${item.value}</div>
      <div class="impact-label th-font">${item.label}</div>
    </div>
  `).join("");
}

function getFocusableElements(container) {
  if (!container) return [];
  return $$('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])', container)
    .filter(el => !el.hasAttribute("disabled"));
}

function trapFocus(event, container) {
  if (event.key !== "Tab") return;

  const focusables = getFocusableElements(container);
  if (!focusables.length) return;

  const first = focusables[0];
  const last = focusables[focusables.length - 1];

  if (event.shiftKey && document.activeElement === first) {
    event.preventDefault();
    last.focus();
  } else if (!event.shiftKey && document.activeElement === last) {
    event.preventDefault();
    first.focus();
  }
}

function openPopup(el) {
  if (!el) return;

  lastFocusedElement = document.activeElement;
  el.style.display = "flex";
  el.setAttribute("aria-hidden", "false");

  requestAnimationFrame(() => el.classList.add("show"));
  lockBodyScroll(true);
  pulseCenter();

  const focusables = getFocusableElements(el);
  if (focusables.length) {
    setTimeout(() => focusables[0].focus(), 50);
  }
}

function closePopup(el, onAfterClose) {
  if (!el) return;

  el.classList.remove("show");
  el.setAttribute("aria-hidden", "true");

  setTimeout(() => {
    el.style.display = "none";

    if (typeof onAfterClose === "function") {
      onAfterClose();
    }

    const infoOpen = infoPopup?.style.display === "flex";
    const companyOpen = companyPopup?.style.display === "flex";

    if (!infoOpen && !companyOpen) {
      lockBodyScroll(false);
    }

    if (lastFocusedElement && typeof lastFocusedElement.focus === "function") {
      lastFocusedElement.focus();
    }
  }, 180);
}

function openInfoPopup(key) {
  const data = popupData[key];
  if (!data) return;

  if (popupKicker) popupKicker.textContent = data.kicker;
  if (infoTitle) infoTitle.textContent = data.title;
  if (infoSubtitle) infoSubtitle.textContent = data.subtitle;
  if (infoLead) infoLead.textContent = data.lead;
  if (videoNote) videoNote.textContent = data.videoNote || "";

  renderBadges(data.badges);
  renderFeatures(data.features, infoFeatures);
  renderImpacts(data.impacts);
  renderFeatures(data.useCases, infoUseCases);

  if (infoVideo) {
    const params = new URLSearchParams({
      rel: "0",
      autoplay: "0",
      mute: "0",
      controls: "1",
      modestbranding: "1",
      playsinline: "1"
    });

    infoVideo.src = `${data.video}?${params.toString()}`;
  }

  openPopup(infoPopup);
}

function closeInfoPopup() {
  closePopup(infoPopup, () => {
    if (infoVideo) infoVideo.src = "";
  });
}

function openCompanyPopup() {
  openPopup(companyPopup);
}

function closeCompanyPopup() {
  closePopup(companyPopup);
}

function buildEnergyLines() {
  if (isMobileView() || !techHero || !logoMenuButton) return;

  const heroRect = techHero.getBoundingClientRect();
  const logoRect = logoMenuButton.getBoundingClientRect();

  const cx = ((logoRect.left + logoRect.width / 2) - heroRect.left) / heroRect.width * 1000;
  const cy = ((logoRect.top + logoRect.height / 2) - heroRect.top) / heroRect.height * 700;

  const sources = $$(".energy-source", techHero);

  sources.forEach((el, index) => {
    const rect = el.getBoundingClientRect();
    const localX = ((rect.left + rect.width / 2) - heroRect.left) / heroRect.width * 1000;
    const localY = ((rect.top + rect.height / 2) - heroRect.top) / heroRect.height * 700;

    const bend = localX < cx ? 72 : -72;
    const midX1 = localX + bend;
    const midX2 = cx - bend * 0.55;
    const d = `M ${localX} ${localY} C ${midX1} ${localY}, ${midX2} ${cy}, ${cx} ${cy}`;

    const line = $(`#line${index + 1}`);
    const run = $(`#run${index + 1}`);

    if (line) line.setAttribute("d", d);
    if (run) run.setAttribute("d", d);
  });
}

function renderAgentResults(keyword = "") {
  if (!agentResults) return;

  const q = keyword.trim().toLowerCase();
  const matches = agentMockData.filter(item => {
    if (!q) return true;
    return (
      item.name.toLowerCase().includes(q) ||
      item.area.toLowerCase().includes(q) ||
      item.code.toLowerCase().includes(q)
    );
  });

  if (!matches.length) {
    agentResults.innerHTML = `<div class="agent-empty th-font">ไม่พบตัวแทนที่ค้นหา</div>`;
    return;
  }

  agentResults.innerHTML = matches.map(item => `
    <button class="agent-item sound-click" type="button" data-agent="${item.name}">
      <div class="agent-name th-font">${item.name}</div>
      <div class="agent-meta th-font">${item.area} • ${item.code}</div>
    </button>
  `).join("");

  $$(".agent-item", agentResults).forEach(btn => {
    btn.addEventListener("click", () => {
      const agent = btn.getAttribute("data-agent") || "";
      window.location.href = `agent-profile.html?agent=${encodeURIComponent(agent)}`;
    });
  });
}

function openAgentDropdown() {
  agentDropdown?.classList.add("open");
}

function closeAgentDropdown() {
  agentDropdown?.classList.remove("open");
}

function toggleHeroOpen(force) {
  if (!techHero) return;

  if (typeof force === "boolean") {
    techHero.classList.toggle("open", force);
  } else {
    techHero.classList.toggle("open");
  }

  syncHeroLabels();
  pulseCenter();
}

function getAudioCtx() {
  if (!audioCtx) {
    const AudioContextClass = window.AudioContext || window.webkitAudioContext;
    if (!AudioContextClass) return null;
    audioCtx = new AudioContextClass();
  }
  return audioCtx;
}

function playClickSound(type = "normal") {
  const ctx = getAudioCtx();
  if (!ctx) return;

  if (ctx.state === "suspended") {
    ctx.resume();
  }

  const now = ctx.currentTime;
  const osc = ctx.createOscillator();
  const gain = ctx.createGain();
  const filter = ctx.createBiquadFilter();

  osc.type = type === "confirm" ? "triangle" : "sine";
  osc.frequency.setValueAtTime(type === "confirm" ? 520 : 680, now);
  osc.frequency.exponentialRampToValueAtTime(type === "confirm" ? 260 : 340, now + 0.045);

  filter.type = "lowpass";
  filter.frequency.setValueAtTime(type === "confirm" ? 1800 : 2400, now);

  gain.gain.setValueAtTime(0.0001, now);
  gain.gain.exponentialRampToValueAtTime(type === "confirm" ? 0.06 : 0.035, now + 0.01);
  gain.gain.exponentialRampToValueAtTime(0.0001, now + (type === "confirm" ? 0.16 : 0.09));

  osc.connect(filter);
  filter.connect(gain);
  gain.connect(ctx.destination);

  osc.start(now);
  osc.stop(now + (type === "confirm" ? 0.18 : 0.1));
}

function applyThemeByViewport() {
  if (isMobileView()) {
    body.classList.add("light-mode");
    return;
  }

  const savedTheme = localStorage.getItem("theme");
  body.classList.toggle("light-mode", savedTheme === "light");
}

function googleTranslateElementInit() {
  if (!window.google?.translate?.TranslateElement) return;

  new google.translate.TranslateElement(
    {
      pageLanguage: "th",
      includedLanguages: "th,en",
      autoDisplay: false,
      layout: google.translate.TranslateElement.InlineLayout.SIMPLE
    },
    "google_translate_element"
  );

  syncActiveLangFromCookie();
}

window.googleTranslateElementInit = googleTranslateElementInit;

function setActiveLang(lang) {
  $$(".lang-btn").forEach(btn => {
    btn.classList.toggle("active", btn.dataset.lang === lang);
  });
}

function getTranslateCookieLang() {
  const match = document.cookie.match(/(?:^|;\s*)googtrans=\/th\/([^;]+)/);
  return match ? decodeURIComponent(match[1]) : "th";
}

function setTranslateCookie(lang) {
  const value = lang === "th" ? "/th/th" : `/th/${lang}`;
  const hostParts = location.hostname.split(".");

  document.cookie = `googtrans=${value};path=/`;

  if (hostParts.length >= 2) {
    const rootDomain = "." + hostParts.slice(-2).join(".");
    document.cookie = `googtrans=${value};path=/;domain=${rootDomain}`;
  }
}

function clearTranslateCookie() {
  const hostParts = location.hostname.split(".");

  document.cookie = "googtrans=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";

  if (hostParts.length >= 2) {
    const rootDomain = "." + hostParts.slice(-2).join(".");
    document.cookie = `googtrans=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=${rootDomain};`;
  }
}

function syncActiveLangFromCookie() {
  const lang = getTranslateCookieLang();
  setActiveLang(["th", "en"].includes(lang) ? lang : "th");
}

function changeLanguage(lang) {
  const combo = $(".goog-te-combo");

  if (lang === "th") {
    clearTranslateCookie();

    if (combo) {
      combo.value = "th";
      combo.dispatchEvent(new Event("change"));
    }

    setActiveLang("th");
    setTimeout(() => location.reload(), 120);
    return;
  }

  setTranslateCookie(lang);
  setActiveLang(lang);

  if (combo) {
    combo.value = lang;
    combo.dispatchEvent(new Event("change"));

    setTimeout(() => {
      if (
        document.body.classList.contains("translated-ltr") ||
        document.body.classList.contains("translated-rtl")
      ) return;

      location.reload();
    }, 700);
  } else {
    location.reload();
  }
}

function bindLanguageButtons() {
  $$(".lang-btn").forEach(btn => {
    btn.addEventListener("click", () => {
      changeLanguage(btn.dataset.lang);
    });
  });
}

function handleResize() {
  clearTimeout(resizeTimer);
  resizeTimer = setTimeout(() => {
    applyThemeByViewport();
    buildEnergyLines();
    syncHeroLabels();
  }, 120);
}

function bindAgentSearch() {
  if (!agentSearchInput || !agentSearchForm) return;

  agentSearchInput.addEventListener("focus", () => {
    renderAgentResults(agentSearchInput.value);
    openAgentDropdown();
  });

  agentSearchInput.addEventListener("input", () => {
    renderAgentResults(agentSearchInput.value);
    openAgentDropdown();
  });

  agentSearchInput.addEventListener("keydown", event => {
    if (event.key === "Escape") {
      closeAgentDropdown();
      agentSearchInput.blur();
    }
  });

  agentSearchForm.addEventListener("submit", event => {
    event.preventDefault();
    const keyword = agentSearchInput.value.trim();

    if (!keyword) {
      renderAgentResults("");
      openAgentDropdown();
      return;
    }

    window.location.href = `agent-profile.html?search=${encodeURIComponent(keyword)}`;
  });
}

function bindHeroMenu() {
  if (logoMenuButton && techHero) {
    logoMenuButton.addEventListener("click", event => {
      event.stopPropagation();
      toggleHeroOpen();
    });
  }

  if (!techHero) return;

  techHero.addEventListener("click", event => {
    event.stopPropagation();
  });

  $$("[data-popup]", techHero).forEach(btn => {
    btn.addEventListener("click", event => {
      event.stopPropagation();
      const key = btn.getAttribute("data-popup");
      openInfoPopup(key);
    });
  });

  if (contactButton) {
    contactButton.addEventListener("click", event => {
      event.stopPropagation();
      openCompanyPopup();
    });
  }
}

function bindPopups() {
  $("#closeInfoPopupTop")?.addEventListener("click", closeInfoPopup);
  $("#closeInfoPopupBottom")?.addEventListener("click", closeInfoPopup);
  $("#closeInfoPopupInline")?.addEventListener("click", closeInfoPopup);

  $("#closeCompanyPopupTop")?.addEventListener("click", closeCompanyPopup);
  $("#closeCompanyPopupBottom")?.addEventListener("click", closeCompanyPopup);

  infoPopup?.addEventListener("click", event => {
    if (event.target === infoPopup) closeInfoPopup();
  });

  companyPopup?.addEventListener("click", event => {
    if (event.target === companyPopup) closeCompanyPopup();
  });

  infoPopup?.addEventListener("keydown", event => {
    trapFocus(event, infoPopup);
  });

  companyPopup?.addEventListener("keydown", event => {
    trapFocus(event, companyPopup);
  });
}

function bindChatWidget() {
  if (!chatWidget || !chatToggle) return;

  chatToggle.addEventListener("click", event => {
    event.stopPropagation();
    chatWidget.classList.toggle("open");
  });
}

function bindSounds() {
  $$(".sound-click").forEach(el => {
    el.addEventListener("click", () => playClickSound("normal"));
  });

  $('.menu-node.bottom[href="book.html"]')?.addEventListener("click", () => {
    playClickSound("confirm");
  });
}

function bindThemeToggle() {
  if (!themeToggle) return;

  themeToggle.addEventListener("click", () => {
    if (isMobileView()) return;

    body.classList.toggle("light-mode");
    localStorage.setItem("theme", body.classList.contains("light-mode") ? "light" : "dark");
  });
}

function bindGlobalEvents() {
  document.addEventListener("pointerdown", event => {
    const target = event.target;

    if (agentDropdown && agentSearchForm) {
      const clickedSearchZone =
        agentDropdown.contains(target) || agentSearchForm.contains(target);

      if (!clickedSearchZone) {
        closeAgentDropdown();
      }
    }

    if (chatWidget && !chatWidget.contains(target)) {
      chatWidget.classList.remove("open");
    }

    if (
      techHero &&
      !techHero.contains(target) &&
      infoPopup?.style.display !== "flex" &&
      companyPopup?.style.display !== "flex"
    ) {
      toggleHeroOpen(false);
    }
  });

  document.addEventListener("keydown", event => {
    if (event.key !== "Escape") return;

    if (infoPopup?.classList.contains("show")) {
      closeInfoPopup();
      return;
    }

    if (companyPopup?.classList.contains("show")) {
      closeCompanyPopup();
      return;
    }

    if (agentDropdown?.classList.contains("open")) {
      closeAgentDropdown();
    }

    if (techHero?.classList.contains("open")) {
      toggleHeroOpen(false);
    }

    if (chatWidget?.classList.contains("open")) {
      chatWidget.classList.remove("open");
    }
  });

  window.addEventListener("resize", handleResize);
}

window.addEventListener("load", () => {
  applyThemeByViewport();

  if (techHero) {
    techHero.classList.remove("open");
  }

  syncHeroLabels();
  buildEnergyLines();
  renderAgentResults("");

  bindLanguageButtons();
  bindAgentSearch();
  bindHeroMenu();
  bindPopups();
  bindChatWidget();
  bindSounds();
  bindThemeToggle();
  bindGlobalEvents();

  syncActiveLangFromCookie();
});
