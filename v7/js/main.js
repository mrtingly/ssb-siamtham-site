const menuToggle=document.getElementById('menuToggle');const mainNav=document.getElementById('mainNav');const protectionButton=document.getElementById('protectionButton');const protectionModal=document.getElementById('protectionModal');const modalClose=document.getElementById('modalClose');const modalExplore=document.getElementById('modalExplore');menuToggle?.addEventListener('click',()=>mainNav?.classList.toggle('open'));document.querySelectorAll('.main-nav a').forEach(link=>link.addEventListener('click',()=>mainNav?.classList.remove('open')));function openProtection(){protectionModal?.classList.add('show');protectionModal?.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeProtection(){protectionModal?.classList.remove('show');protectionModal?.setAttribute('aria-hidden','true');document.body.style.overflow=''}protectionButton?.addEventListener('click',openProtection);modalClose?.addEventListener('click',closeProtection);modalExplore?.addEventListener('click',closeProtection);protectionModal?.addEventListener('click',event=>{if(event.target===protectionModal)closeProtection()});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeProtection()});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(element=>observer.observe(element));window.addEventListener('scroll',()=>{document.querySelector('.site-header')?.classList.toggle('scrolled',window.scrollY>20)},{passive:true});